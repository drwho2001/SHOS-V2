// Ambient declaration for the one real Vite `define` build-time
// constant this app uses (see vite.config.js) — not a real value that
// exists anywhere at runtime type-checking would otherwise see.
declare const __BUILD_SHA__: string;
