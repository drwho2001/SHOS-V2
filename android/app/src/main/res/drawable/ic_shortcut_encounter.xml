<?xml version="1.0" encoding="utf-8"?>
<!-- App Shortcut icon for "Add encounter" — same filled/coloured
     convention as ic_shortcut_medication.xml, see its own comment.
     Encounters' own accent as the circular background, a plain "+"
     glyph in white — matching the same "+" this app already uses
     everywhere else for "add new" (the FAB), not inventing a new
     symbol just for this. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="48dp"
    android:height="48dp"
    android:viewportWidth="48"
    android:viewportHeight="48">
    <path
        android:fillColor="#8D3B7A"
        android:pathData="M24,2A22,22 0,0 1,24 46A22,22 0,0 1,24 2Z" />
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M20,12H28V20H36V28H28V36H20V28H12V20H20Z" />
</vector>
